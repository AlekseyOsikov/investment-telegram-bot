# Tasks

## 1. Конфигурация

- [x] 1.1 Добавить в `src/config.py` переменные `PRICE_WATCH` (булев флаг по образцу `RESEARCH`, по умолчанию включён), `PRICE_WATCH_DB` (по умолчанию `data/price_watch/watch.sqlite3`, приводится к абсолютному пути), `PRICE_WATCH_TIMEOUT_SECONDS` (по умолчанию 90) и производную `PRICE_WATCH_ACTIVE = PRICE_WATCH and bool(MCP_MOEX_DIR)`; проверку значений добавить в `_validate_config()`; проверить `python3 -m py_compile src/config.py` и запуском с `PRICE_WATCH=maybe` (процесс завершается с сообщением о допустимых значениях) и с `PRICE_WATCH_TIMEOUT_SECONDS=0` (то же)
- [x] 1.2 Документировать три переменные в `.env.example` (назначение, значения по умолчанию, пометка «настройка оператора», условие активности через `MCP_MOEX_DIR`); проверить, что описание рядом с `MCP_MOEX_DIR` согласовано

## 2. Защита модели: список разрешённых инструментов

- [x] 2.1 В `src/mcp_integration/market_session.py` добавить перечень разрешённых инструментов (`search_securities`, `get_current_price`, `get_price_history`) и использовать его вместе с проверкой read-only в `MarketTools` (пропущенные инструменты называются в предупреждении журнала); проверить в `tests/test_market_tools.py`: инструмент из перечня с read-only проходит, read-only вне перечня, инструмент из перечня без пометки и инструменты `watch_*` не отдаются модели, а вызов такого по имени возвращается как ошибка без обращения к серверу
- [x] 2.2 Проверить тестом, что команда запуска сервера для ответа smart-агента не содержит `--watch-db` (вынести сборку параметров в чистую функцию, если её ещё нет, без изменения поведения); существующие тесты `tests/test_market_tools.py` проходят

## 3. Обращения к серверу расписания

- [x] 3.1 Создать `src/mcp_integration/watch_session.py`: чистая функция сборки параметров запуска (`uv run --directory <MCP_MOEX_DIR> mcp-moex --watch-db <абсолютный путь>` списком, без shell), чистая функция «результат инструмента → словарь» (структурный результат, ошибка сервера → `WatchToolError` с текстом сервера, пустой результат → понятная ошибка), асинхронный вызов одного инструмента (один процесс на обращение, тайм-аут на весь цикл, разворачивание `ExceptionGroup` из одного исключения); проверить `tests/test_watch_session.py` на чистых функциях: путь абсолютный даже при относительном значении конфигурации, каталог с пробелами и кавычками не ломает список аргументов, результат разбирается на настоящих типах `mcp.types`, ошибка сервера сохраняет текст
- [x] 3.2 Проверить вручную на живом `mcp-moex` (со свежей реализацией `--watch-db`): вызов `watch_status` для неизвестного чата возвращает `{"active": false}`, а процесс сервера после вызова не остаётся в списке процессов; с неверным `MCP_MOEX_DIR` вызов даёт `FileNotFoundError` или ошибку соединения, а не зависание

## 4. Разбор команды и тексты

- [x] 4.1 Создать `src/price_watch/parse.py`: разбор аргументов `/watch` (запятые и пробелы, два последних слова периоды, `m/h/d` и `м/ч/д` без учёта регистра с переводом в латиницу, тикеры остальное) с результатом «значения или причина формы»; проверить `tests/test_price_watch_parse.py`: `SBER GAZP 15m 1h`, `sber, gazp 15м 1ч`, `SBER 15m` и `SBER GAZP` (ошибка формы), пустые аргументы, период не в конце, `1D`, `15 m`, «18:00», ведущие и двойные запятые
- [x] 4.2 Создать `src/price_watch/texts.py`: подсказка с форматом и примерами, ответ на `/watch` (тикеры, периоды, признак замены, сроки по МСК, первые цены с пометкой о задержке), ответ `/watch_status` (включая «опроса нет»), ответ `/watch_stop`, форматирование ISO-времени сервера в `ДД.ММ ЧЧ:ММ (МСК)`; проверить `tests/test_price_watch_texts.py` на образцах ответов сервера из его контракта (включая замену опроса, облигацию и индекс в первых ценах, `active: false`)

## 5. Планировщик

- [x] 5.1 Создать `src/price_watch/scheduler.py` с функцией одного тика (внедряемые `call_tool` и `send`): выдача сводок по порядку с разбиением по `TELEGRAM_MESSAGE_LIMIT`, подтверждение только отправленных, остановка опроса чата при «бот заблокирован»/«чат не найден», сбой одного чата не мешает остальным, `has_more` даёт немедленное повторение, временный сбой отправки оставляет сводку неподтверждённой и просит повтор; проверить `tests/test_price_watch_scheduler.py` на фейках: успех, два чата с разными исходами, `Forbidden`, сетевая ошибка, длинный текст (несколько сообщений), `has_more`, пустой ответ
- [x] 5.2 Добавить в `scheduler.py` политику следующего шага и класс цикла (`run`, `schedule_at`, `stop`): срок из `next_due_at`, контрольная сверка не реже раза в час, немедленный первый тик при запуске, пауза повтора при сбое тика 60 с с удвоением до 900 с и сбросом после успеха, `schedule_at` переносит срок только на более ранний момент и будит цикл; проверить в том же тестовом файле на подменённых часах и ожидании без реального сна: первый тик сразу, `next_due_at = null` ведёт к контрольной сверке, перенос вперёд игнорируется, перенос назад срабатывает, серия сбоев растит паузу до предела, успех её сбрасывает, отмена посреди тика завершает цикл без исключения наружу

## 6. Команды

- [x] 6.1 Создать `src/price_watch/commands.py`: обработчики `/watch`, `/watch_status`, `/watch_report`, `/watch_stop` (`CommandHandler` с фильтром личного чата, `chat_id` из `update.effective_chat.id`), перевод ошибок в русские сообщения отдельными `except` по образцу `mcp_integration/tools_command.py` (`FileNotFoundError`, `TimeoutError`, `WatchToolError` с текстом сервера, последний рубеж с `logger.exception`), передача сроков из ответа `/watch` в планировщик через `application.bot_data`, фабрики `build_*_handler()` или одна `build_price_watch_handlers()`; проверить `python3 -m py_compile` и ручным прогоном (задача 8.2), так как обработчики не покрываются юнит-тестами
- [x] 6.2 Убедиться при чтении `commands.py`, что ни одна ветка не передаёт серверу ничего кроме `chat_id`, тикеров и периодов, не вызывает LLM и не читает память агентов; проверить поиском по файлу (`rg "main_client|SmartAgent|Agent\("` не находит совпадений в `src/price_watch/`)

## 7. Подключение к боту

- [x] 7.1 В `src/main.py` при `PRICE_WATCH_ACTIVE` регистрировать обработчики команд, создавать планировщик в `post_init` (положить в `application.bot_data`, запустить задачу) и отменять в `post_shutdown`; проверить `python3 -m py_compile src/main.py` и запуском с незаданным `MCP_MOEX_DIR` (команды не зарегистрированы, бот работает как прежде) и с заданным (в журнале видно первый тик при старте)
- [x] 7.2 Дополнить `/help` (четыре команды с форматом `/watch`, только при `PRICE_WATCH_ACTIVE`) и `/start` (бот сам присылает сводки после `/watch`, хранит список тикеров чата и публичные цены, удаляется `/watch_stop`; обычные сообщения по-прежнему не запоминаются); проверить чтением текстов при включённой и выключенной возможности и что итоговый `/help` по-прежнему режется на части корректно

## 8. Документация и сквозная проверка

- [x] 8.1 Обновить `README.md` (команды, настройка, пример) и `CLAUDE.md`: структура (`price_watch/`, `mcp_integration/watch_session.py`), конфигурация, отступление от «бот только отвечает» (бот пишет сам после явной команды) в «Ограничениях безопасности» и «Правилах предметной области», перечень разрешённых инструментов в разделе про слой `tools`, список `py_compile`, описание тестов; проверить, что формулировки согласованы со спеками этого изменения
- [x] 8.2 Сквозная ручная проверка на живом `mcp-moex`: `/watch SBER GAZP 5m 5m` (ответ с первыми ценами и сроками), `/watch_status`, через ≈5 минут приходит сводка, `/watch_report`, повторный `/watch` с другими тикерами (замена), перезапуск бота между сводками (сводка приходит один раз), блокировка бота пользователем (опрос останавливается, в журнале видно причину), `/watch_stop`; вопрос smart-агенту о цене работает как прежде и не видит инструментов `watch_*` (`/smart_agent_show`)
- [x] 8.3 Прогнать `make test` и `openspec validate add-price-watch-commands`; проверить, что все тесты проходят, а `ruff check src/price_watch src/mcp_integration/watch_session.py` не даёт замечаний в новых файлах
