# Tasks

## 1. Зависимости и окружение

- [x] 1.1 Добавить `mcp` в `requirements.txt` с границами версий в стиле существующих
      строк (`openai>=1.30.0,<2`); нижнюю границу взять по фактически установленной
      версии. Проверка: `pip install -r requirements.txt` в чистом venv проходит без
      конфликтов версий `httpx`/`anyio`/`pydantic`, а `python3 -c "import mcp, openai,
      telegram"` отрабатывает молча.
- [x] 1.2 Убедиться, что тестовый MCP-сервер стартует на хосте: `npx -y
      @modelcontextprotocol/server-everything --help` (или запуск без аргументов с
      последующим прерыванием) завершается без ошибки и пакет попадает в кэш npx.

## 2. Конфигурация

- [x] 2.1 Добавить в `config.py` переменные `MCP_SERVER_COMMAND` (по умолчанию `npx`),
      `MCP_SERVER_ARGS` (по умолчанию `-y @modelcontextprotocol/server-everything`,
      разбор через `shlex.split`) и `MCP_TIMEOUT_SECONDS` (по умолчанию 60) с
      комментариями, объясняющими, почему команда и аргументы разделены и почему это
      настройка оператора, а не пользователя чата (design.md, решение 5). Проверка:
      `python3 -c "import sys; sys.path.insert(0,'src'); import config;
      print(config.MCP_SERVER_COMMAND, config.MCP_SERVER_ARGS,
      config.MCP_TIMEOUT_SECONDS)"` печатает значения по умолчанию, а переопределение
      их в `.env` меняет вывод.
- [x] 2.2 Документировать все три переменные в `.env.example` в стиле остальных
      секций, включая требование Node.js/`npx` на хосте и подсказку увеличить
      тайм-аут, если первый запуск скачивает пакет. Проверка: секция присутствует, а
      имена переменных совпадают с прочитанными в `config.py`.

## 3. Слой подключения к MCP

- [x] 3.1 Создать пакет `src/mcp_integration/` (`__init__.py`) с докстрингом,
      объясняющим, почему пакет называется не `mcp` (затенил бы SDK при запуске
      `python src/main.py`, design.md, решение 1). Проверка: `python3 -c "import sys;
      sys.path.insert(0,'src'); import mcp_integration, mcp; print(mcp.__file__)"`
      печатает путь к SDK, а не к каталогу проекта.
- [x] 3.2 Реализовать в `src/mcp_integration/client.py` dataclass-результат (имя и
      версия сервера, список инструментов с `name`/`description`/`required_params`) и
      асинхронную функцию подключения: `stdio_client(StdioServerParameters(...))` +
      `ClientSession.initialize()` + `list_tools()` внутри одного `async with`, весь
      блок обёрнут в `asyncio.timeout(MCP_TIMEOUT_SECONDS)`; исключения наружу не
      перехватываются (design.md, решения 2–4). Проверка: временный скрипт, вызывающий
      эту функцию через `asyncio.run`, печатает непустой список инструментов
      `server-everything`.
- [x] 3.3 Убедиться, что дочерний процесс не переживает вызов: после успешного запуска
      и после принудительного тайм-аута (временно выставить `MCP_TIMEOUT_SECONDS=1`)
      `pgrep -f server-everything` ничего не находит.
- [x] 3.4 Вынести форматирование результата в чистую функцию «результат → текст»
      (количество инструментов, имя/версия сервера, по инструменту — имя, описание,
      обязательные параметры из `inputSchema["required"]`, усечение длинных описаний,
      отдельный текст для пустого списка). Проверка: функция не импортирует `telegram`
      и вызывается в тестах без сети.

## 4. Команда бота `/mcp_tools`

- [x] 4.1 Реализовать `src/mcp_integration/tools_command.py`: обработчик команды
      (`ChatAction.TYPING` + сообщение «подключаюсь…», вызов слоя из 3.2, отправка
      отформатированного текста без `parse_mode` с нарезкой по
      `TELEGRAM_MESSAGE_LIMIT`), игнорирование любых аргументов сообщения, и
      `build_mcp_tools_handler() -> CommandHandler` по образцу
      `build_agent_history_handler()`. Проверка: `python3 -m py_compile` проходит, а
      `/mcp_tools` в личном чате присылает список инструментов.
- [x] 4.2 Перевести сбои в сообщения на русском отдельными `except`: отсутствие
      программы запуска (`FileNotFoundError`), тайм-аут (`TimeoutError`), ошибки
      протокола/аварийное завершение сервера, общий fallback с `logger.exception` —
      тем же паттерном, что `handle_message` в `main.py`, без переиспользования
      `research/_shared.api_error_to_message`. Проверка: с
      `MCP_SERVER_COMMAND=nosuchbinary` бот отвечает сообщением о том, что сервер не
      удалось запустить; с `MCP_TIMEOUT_SECONDS=1` — сообщением о тайм-ауте; в обоих
      случаях следующий обычный вопрос обрабатывается как раньше.
- [x] 4.3 Зарегистрировать обработчик в `main.py` внутри существующего блока
      `if RESEARCH_ENABLED` и добавить строку про `/mcp_tools` в список команд
      `help_command` рядом с `/research_*`. Проверка: при `RESEARCH=true` команда
      работает и видна в `/help`; при `RESEARCH=false` бот её молча игнорирует и в
      `/help` не упоминает.

## 5. Тесты

- [x] 5.1 Добавить `tests/test_mcp_formatting.py` на чистую функцию из 3.4: непустой
      список, пустой список, инструмент без описания, обязательные параметры из
      `inputSchema`, усечение длинного описания. Проверка: `pytest tests/` проходит
      целиком, включая существующие тесты автомата задачи; моков сессии MCP и сетевых
      вызовов в тестах нет.

## 6. Документация и финальная проверка

- [x] 6.1 Обновить `CLAUDE.md`: новый пакет `mcp_integration/` в списке модулей, новая
      команда в перечне и в списке команд под флагом `RESEARCH`, новые переменные
      окружения в разделе «Конфигурация», строка про `src/mcp_integration/*.py` в
      команде проверки синтаксиса `py_compile`. Проверка: перечисленные места
      действительно упоминают MCP, а команда `py_compile` из CLAUDE.md выполняется без
      ошибок.
- [x] 6.2 Обновить `README.md`: команда `/mcp_tools`, требование Node.js/`npx` на
      хосте, новые переменные окружения. Проверка: описание совпадает с `.env.example`
      и текстом `/help`.
- [x] 6.3 Прогнать `ruff check src/mcp_integration/ src/main.py src/config.py` и
      убедиться, что новые файлы не добавляют замечаний (нулевой результат по всему
      репозиторию не требуется — см. CLAUDE.md), затем вручную проверить сценарии
      спеки: успешный список, сервер без инструментов (или сторонний stdio-сервер
      через `MCP_SERVER_COMMAND`/`MCP_SERVER_ARGS`), отсутствующая программа запуска,
      тайм-аут, `RESEARCH=false`.
