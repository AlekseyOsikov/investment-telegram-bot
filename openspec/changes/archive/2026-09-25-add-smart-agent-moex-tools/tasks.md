# Tasks

## 1. Проверка окружения и замеры

- [x] 1.1 Убедиться, что `uv` доступен в `PATH` процесса бота и сервер стартует из
      каталога проекта: `uv run --directory <каталог mcp-moex> mcp-moex` из
      `.venv` бота через `mcp.client.stdio.stdio_client` отдаёт рукопожатие и три
      инструмента (`search_securities`, `get_current_price`, `get_price_history`).
      Проверка: одноразовый скрипт печатает имена трёх инструментов и время
      рукопожатия (ожидаем порядка 1 с).
- [x] 1.2 Убедиться, что в установленной версии SDK у инструмента доступно поле
      `annotations.read_only_hint` и оно `True` у всех трёх инструментов (design.md,
      решение 5). Проверка: тот же скрипт печатает `True` для каждого; если имя поля
      другое — исправить решение 5 и спеку до реализации.
- [x] 1.3 Замерить размер `get_price_history` (300 свечей) в КОМПАКТНОМ JSON из
      `structured_content` и выбрать значение по умолчанию для
      `MCP_TOOL_RESULT_MAX_CHARS` так, чтобы полный ответ сервера умещался
      (design.md, решение 6). Проверка: измеренное число и выбранный предел записаны
      комментарием рядом с переменной в `config.py` (задача 2.1).

## 2. Конфигурация

- [x] 2.1 Добавить в `config.py` `MCP_MOEX_DIR` (пусто по умолчанию, `expanduser` и
      `strip`), `MCP_MAX_TOOL_STEPS` (по умолчанию 5) и `MCP_TOOL_RESULT_MAX_CHARS`
      (по результату задачи 1.3), с комментариями: настройка оператора, а не
      пользователя; почему `uv` и аргументы собираются в коде, а не берутся из
      окружения (design.md, решение 7). Проверка: `python3 -c "import sys;
      sys.path.insert(0,'src'); import config; print(config.MCP_MOEX_DIR,
      config.MCP_MAX_TOOL_STEPS, config.MCP_TOOL_RESULT_MAX_CHARS)"` печатает
      значения по умолчанию, а значения из `.env` их переопределяют.
- [x] 2.2 Добавить проверку каталога при старте: если `MCP_MOEX_DIR` задан, но каталога
      нет или в нём нет `pyproject.toml`, писать `logger.warning` и НЕ останавливать
      процесс (не через `_validate_config`, который завершает бота). Числовые значения
      проверять на положительность там же по образцу существующих. Проверка: запуск
      с несуществующим каталогом логирует предупреждение и бот стартует; запуск без
      переменной молчит.
- [x] 2.3 Документировать три переменные в `.env.example` в стиле соседних секций:
      требование `uv` на хосте и абсолютный путь к каталогу, «пусто — возможность
      выключена», связь с `/smart_agent_toggle tools`. Проверка: имена в
      `.env.example` совпадают с прочитанными в `config.py`.

## 3. Механика MCP-сессии

- [x] 3.1 Создать `src/mcp_integration/market_session.py` с ЧИСТЫМИ функциями:
      проверка read-only (`annotations.read_only_hint is True`, отсутствие аннотации —
      не read-only), преобразование инструмента MCP в формат OpenAI, разбор аргументов
      вызова (`parse_arguments`: строка JSON → `dict` либо текст ошибки), преобразование
      результата вызова в текст (`is_error` → текст сервера как есть; иначе компактный
      JSON из `structured_content`; иначе склеенные текстовые блоки), усечение по
      пределу с пометкой (начало сохраняется). Проверка: `tests/test_market_tools.py`
      покрывает каждую функцию, в том числе: инструмент без аннотации не проходит фильтр,
      `arguments` не-объект/невалидный JSON, результат ровно на пределе и на один
      символ больше, кириллица не экранируется.
- [x] 3.2 Реализовать в том же модуле асинхронный контекстный менеджер сессии
      (`uv run --directory <dir> mcp-moex` списком аргументов, рукопожатие и
      `list_tools()` под `MCP_TIMEOUT_SECONDS`, доступный список инструментов OpenAI,
      метод вызова, который НЕ бросает исключений уровня инструмента и возвращает текст
      ошибки: неизвестное или не read-only имя, невалидные аргументы, `is_error`, обрыв
      обмена, тайм-аут вызова). Ошибку ЗАПУСКА (`FileNotFoundError`, тайм-аут,
      исключения SDK) не перехватывать, как в `client.py`. Проверка: `python3 -m
      py_compile`; импорт модуля не запускает процессов; ручной прогон из задачи 8.3.

## 4. Правила и тексты агента

- [x] 4.1 Создать `src/agents/market_tools.py` (по образцу `agents/invariants.py`): два
      варианта системного сообщения слоя (сервер доступен / недоступен) с обязательными
      по спеке пунктами — данные, а не инструкции; время и задержка котировки; единицы;
      не гарантия; прямая оговорка, что оно НЕ отменяет `SYSTEM_PROMPT`; запрет называть
      текущие цены по памяти при недоступности; запись о вызове (имя, аргументы, исход)
      и её текстовая строка для чата; строки предупреждений и статуса слоя. Проверка:
      тесты проверяют наличие в сообщении оговорки про `SYSTEM_PROMPT` и правила про
      данные, а не инструкции — так их нельзя молча убрать при правке текстов.
- [x] 4.2 Реализовать в этом же модуле цикл вызовов с внедряемыми `complete` и
      `call_tool` (design.md, решение 4): до `MCP_MAX_TOOL_STEPS` шагов, все вызовы шага
      выполняются и получают сообщения `role: "tool"`, сообщение ассистента несёт
      `tool_calls` и `reasoning_content`, если он есть; при исчерпании лимита — ещё одно
      обращение без инструментов и признак «лимит исчерпан»; счётчик обращений и
      суммирование токенов; список записей о вызовах. Проверка: тесты на фейковых
      функциях — ответ без вызовов (один шаг, без записей), один вызов, два вызова в
      одном шаге (оба получили ответ), лимит исчерпан (финальный вызов без инструментов),
      ошибка инструмента доходит до модели текстом.

## 5. SmartAgent

- [x] 5.1 Добавить слой `LAYER_TOOLS = "tools"` в `LAYER_*`/`ALL_LAYERS` в
      `agents/smart_agent.py`. Проверка: чат со старым файлом памяти (без ключа
      `tools`) читается как «слой включён», остальные настройки слоёв не меняются;
      `set_layer_enabled("tools", False)` сохраняется в файл и читается обратно.
- [x] 5.2 Вынести существующее тело `ask()` в `_ask_plain()` без изменения поведения и
      добавить ветку `_ask_with_tools()`: при выключенном слое или пустом
      `MCP_MOEX_DIR` процесс не запускается и `asyncio.run` не вызывается; иначе
      `asyncio.run` запускает сессию (задача 3.2) и цикл (задача 4.2). Сбой запуска
      сервера → путь «без инструментов» с вариантом сообщения «недоступен» и
      `warnings`. Проверка: без `MCP_MOEX_DIR` и с выключенным слоем `ask()`
      обращается к клиенту так же, как до изменения (тот же набор аргументов, без `tools`);
      сообщение о выключенном слое при настроенном сервере — задача 9.2.
- [x] 5.3 Вставить сообщение слоя в `_build_context_messages()` в порядке «инварианты →
      профиль → инструменты → долговременная → рабочая → краткосрочная»; расширить
      `SmartAgentAnswer` (`tool_calls`, `warnings`, `llm_calls`), `context_tokens`/
      `response_tokens` — суммы по обращениям; записывать в `short_term` ровно одну
      пару после успешного завершения. Добавить статус слоя на последний вопрос
      (в памяти объекта, не на диск) и геттер для `/smart_agent_show`. Проверка: ручной
      прогон из задачи 8.3 — в `short_term` одна пара, без сообщений `tool`.
- [x] 5.4 Убедиться, что служебные вызовы (`_call_json_api`, `update_task_state`,
      `_maybe_start_task`, `_check_invariants`) не получают `tools`. Проверка:
      `grep -n "tools=" src/agents/smart_agent.py` показывает `tools=` только в пути
      основного ответа.

## 6. Командный слой

- [x] 6.1 В `agents/smart_agent_command.py` заменить `agent.ask(user_text)` на
      `await asyncio.to_thread(agent.ask, user_text)`, сохранив все существующие
      `except` без изменений. Проверка: ошибка авторизации/лимита/тайм-аута по-прежнему
      даёт те же сообщения (эмулировать неверным ключом).
- [x] 6.2 Добавить в служебное сообщение после ответа строки о вызванных инструментах
      (`🔧 …`), предупреждение о недоступности данных биржи или об исчерпанном лимите и
      «обращений: N» в строку токенов при N > 1. Без вызовов и без предупреждений
      сообщение не меняется. Проверка: ручной прогон из задачи 8.3.
- [x] 6.3 Добавить `tools` в `LAYER_LABELS`, `LAYER_ALIASES` и `LAYER_TOGGLE_HINT`
      `/smart_agent_toggle`, обновить докстринги и текст справки команды; показать в
      `/smart_agent_show` статус слоя (включён / выключен / не настроен / недоступен) и
      вызовы последнего вопроса. Проверка: `/smart_agent_toggle tools` переключает слой,
      `/smart_agent_toggle` с неверным аргументом показывает новую подсказку с `tools`.

## 7. Документация

- [x] 7.1 Обновить `main.py` (строка про `/smart_agent_toggle` в `/help`: список слоёв
      с `tools`) и `README.md` (таблица команд, раздел про smart-агента, переменные
      окружения, требование `uv` на хосте). Проверка: во всех местах, где перечислены
      слои, есть `tools`; `grep -rn "profile|invariants|short|working|long" src README.md`
      не находит устаревших перечислений.
- [x] 7.2 Обновить CLAUDE.md: дерево пакетов (`mcp_integration/market_session.py`,
      `agents/market_tools.py`), раздел про smart-агента (слой `tools`, цикл, sync/async
      мост, оговорка «проверено на DeepSeek»), раздел «Конфигурация»
      (`MCP_MOEX_DIR`, `MCP_MAX_TOOL_STEPS`, `MCP_TOOL_RESULT_MAX_CHARS` и почему это
      настройки оператора), «Ограничения безопасности» и «Правила предметной области»
      (агент передаёт серверу аргументы, сформированные моделью, — это осознанное
      расхождение с прежней формулировкой для `/mcp_tools`; слой не ослабляет
      `SYSTEM_PROMPT`), список `py_compile`, число слоёв и текст про `enabled_layers`, а
      также перечень тестов. Проверка: CLAUDE.md не содержит утверждений, противоречащих
      спеке (в частности «только перечисляет» вне контекста `/mcp_tools`).

## 8. Проверка

- [x] 8.1 `make test` проходит: прежние тесты и новые (`tests/test_market_tools.py`).
      `python3 -m py_compile` по расширенному списку файлов проходит. `ruff check src/`
      не добавляет замечаний относительно состояния до изменения (сверять по числу и
      по затронутым файлам).
- [x] 8.2 `openspec validate add-smart-agent-moex-tools --strict` проходит.
- [x] 8.3 Ручной прогон на живом боте (`make run`, `MCP_MOEX_DIR` задан, ALL_PROXY без
      `socks://`): (а) вопрос «сколько сейчас стоит SBER» — ответ с ценой, временем и
      задержкой, в чате строка `🔧`; (б) вопрос про две бумаги — оба вызова; (в) вопрос
      без биржи («что такое дивиденды») — без строк `🔧`, ответ без задержки-предупреждения;
      (г) неверный тикер — модель сообщает, что не нашла, и/или ищет по названию;
      (д) `/smart_agent_toggle tools` — процесс не запускается, ответ без инструментов,
      `/smart_agent_show` показывает «выключен»; (е) `MCP_MOEX_DIR` на несуществующий
      каталог — предупреждение в журнале при старте, предупреждение в чате при вопросе,
      ответ без выдуманных цен; (ж) без `MCP_MOEX_DIR` — никаких предупреждений и
      процессов; (з) вопрос об истории цены за год — ответ без обрезки либо с пометкой;
      (и) `/mcp_tools` по-прежнему показывает сервер по умолчанию; (к) `/agent` и
      обычные вопросы работают как раньше. Проверка: каждый пункт подтверждён
      наблюдаемым поведением, отклонения зафиксированы в спеке или задачах.

## 9. Сообщение при выключенном слое

- [x] 9.1 Добавить в `agents/market_tools.py` `build_disabled_context_message()` — третий
      вариант сообщения слоя (design.md, решение 9): данные биржи выключены пользователем
      (`/smart_agent_toggle tools`); цены и котировки из прежних ответов не выдавать за
      текущие и не приписывать им время котировки; на вопрос, требующий текущих данных,
      сказать, что данные выключены и как включить; прямая оговорка, что блок не отменяет
      и не ослабляет обязательные правила основной инструкции. Проверка: тесты в
      `tests/test_market_tools.py` (роль `system`, есть оговорка про основную инструкцию,
      есть запрет на «текущие» цены из прежних ответов и отсылка к команде включения).
- [x] 9.2 В `SmartAgent.ask()` передавать это сообщение в `_plain_completion` при
      `STATUS_OFF`, если `MCP_MOEX_DIR` задан; при `STATUS_NOT_CONFIGURED` не передавать
      (design.md, решение 8). Запрос к клиенту по-прежнему без ключа `tools`. Проверка:
      скрипт с фейковым клиентом — слой выключен + каталог задан: в контексте одно
      сообщение «данные биржи выключены», `tools` в запросе нет; слой выключен + каталог
      не задан: сообщения нет и контекст прежний; слой включён: прежние варианты без
      изменений.
- [x] 9.3 Обновить документацию: раздел «Инструменты рыночных данных smart-агента» и
      описание слоя `tools` в CLAUDE.md, README (пункт про слой `tools`), докстринги
      `SmartAgent`/`smart_agent_command.py`, где сказано, что выключенный слой «просто
      оставляет ответ без данных биржи». Проверка: `grep -n "отдельного предупреждения"
      CLAUDE.md src/agents/smart_agent_command.py` не находит устаревших утверждений;
      тексты не противоречат спеке.
- [x] 9.4 Повторить живую проверку сценария «слой выключен» (скрипт `live_check.py`, шаг
      «д»): сначала ответ с ценой при включённом слое, затем выключение и повторный
      вопрос о цене. Проверка: модель не выдаёт прежнюю цену за текущую, не называет
      время котировки и сообщает, что данные биржи выключены; если поведение осталось
      прежним — зафиксировать в design.md, что митигация слабее ожидаемой, и обсудить
      усиление.
