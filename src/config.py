"""Общая конфигурация, логирование и настройки, не привязанные к конкретному провайдеру.

Подключения к LLM-провайдерам (API-ключ, базовый URL, клиент openai.OpenAI, идентификаторы
моделей) вынесены в отдельные модули — providers/deepseek_client.py и
providers/kimi_client.py. Какой из них обслуживает основной поток бота, определяет
MAIN_CLIENT (см. ниже) — от него зависит, какой из двух API-ключей обязателен для
старта, а какой нужен только техническому режиму /research_models и не должен блокировать
запуск остального бота (эту проверку выполняет каждый клиентский модуль сам, см. их
докстринги). Сам выбор клиента для основного потока (объект main_client) собирается в
providers/main_client.py — не здесь, чтобы не создавать цикл импорта:
providers/deepseek_client.py и providers/kimi_client.py импортируют config.py ради
побочного эффекта (load_dotenv() и logging.basicConfig() должны отработать раньше, чем
они читают переменные окружения и логируют), поэтому только config.py вызывает
load_dotenv(), а сам он не может импортировать их в ответ.
"""

from __future__ import annotations

import logging
import os
import sys
import warnings

from dotenv import load_dotenv
from telegram.warnings import PTBUserWarning

load_dotenv()

TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

# Провайдер, обслуживающий основной поток бота (main.py) и research-режимы, которые сами
# не выбирают конкретную модель (research/reasoning.py, research/temperature.py,
# research/constraints.py) — все они используют providers/main_client.py.
# research/models.py не зависит от MAIN_CLIENT: он всегда сравнивает обе пары моделей
# обоих провайдеров.
_SUPPORTED_MAIN_CLIENTS = ("deepseek", "kimi")
MAIN_CLIENT = os.getenv("MAIN_CLIENT", "deepseek").strip().lower()

# Модель основного потока — единственная переменная, а не своя на каждого провайдера,
# т.к. одновременно обслуживает только один из них (тот, что выбран в MAIN_CLIENT).
# Дефолт зависит от MAIN_CLIENT, чтобы работать "из коробки" при любом выборе провайдера.
_DEFAULT_MAIN_MODEL_BY_CLIENT = {"deepseek": "deepseek-v4-flash", "kimi": "kimi-k3"}
MAIN_MODEL = os.getenv(
    "MAIN_MODEL", _DEFAULT_MAIN_MODEL_BY_CLIENT.get(MAIN_CLIENT, "deepseek-v4-flash")
)

# Только для сообщений пользователю/логов основного потока (main.py и три research-режима
# из комментария выше) — чтобы они называли реально выбранного провайдера, а не всегда
# "DeepSeek", и подсказывали правильную переменную окружения при ошибке авторизации.
MAIN_CLIENT_LABEL = "DeepSeek" if MAIN_CLIENT == "deepseek" else "Kimi"
MAIN_API_KEY_ENV_VAR = "DEEPSEEK_API_KEY" if MAIN_CLIENT == "deepseek" else "KIMI_API_KEY"

# Доступ к исследовательским/техническим командам — /research_*, /agent_compare*,
# /agent_mode, /agent_context. Это инструменты отладки и экспериментов с API, а не
# часть основного сценария использования бота (см. "Правила предметной области" в
# CLAUDE.md) — оператор бота может скрыть их полностью на проде через RESEARCH=false:
# такие команды не регистрируются как обработчики (main.py) и не упоминаются в
# /help. НЕ относится к /agent (сам агент), /agent_reset/agent_history (базовое
# управление его историей) и /agent_checkpoint/agent_branch/agent_switch_branch
# (управление ветками — часть функциональности самого агента, а не эксперимент).
_TRUE_VALUES = {"true", "1", "yes", "on"}
_FALSE_VALUES = {"false", "0", "no", "off"}
_RESEARCH_RAW = os.getenv("RESEARCH", "true").strip().lower()
RESEARCH_ENABLED = _RESEARCH_RAW in _TRUE_VALUES

# Разумные лимиты, чтобы не улететь по токенам/времени на один запрос.
REQUEST_TIMEOUT_SECONDS = float(os.getenv("REQUEST_TIMEOUT_SECONDS", "60"))
MAX_OUTPUT_TOKENS = int(os.getenv("MAX_OUTPUT_TOKENS", "2000"))
MAX_INPUT_CHARS = int(os.getenv("MAX_INPUT_CHARS", "4000"))

# Каталог для JSON-файлов истории диалога агента (agents/agent.py), один файл на
# chat_id. Единственное место в проекте, где история диалога сохраняется на диск —
# см. «Ограничения безопасности» в CLAUDE.md про то, почему это осознанное
# исключение, а не общее правило для всего бота.
AGENT_HISTORY_DIR = os.getenv("AGENT_HISTORY_DIR", "data/agent_history")

# Управление контекстом агента (agents/agent.py): вместо передачи в LLM всей
# сохранённой истории целиком, более старая её часть периодически сворачивается в
# текстовую сводку (summary), а «свежий хвост» (минимум AGENT_CONTEXT_RECENT_PAIRS
# последних пар вопрос-ответ, плюс ещё до AGENT_SUMMARY_CHUNK_PAIRS-1 пар, ещё не
# набравших полный блок на свёртку) передаётся как есть. Подробности алгоритма — в
# докстринге Agent._maybe_update_summary (agents/agent.py).
AGENT_CONTEXT_RECENT_PAIRS = int(os.getenv("AGENT_CONTEXT_RECENT_PAIRS", "10"))
AGENT_SUMMARY_CHUNK_PAIRS = int(os.getenv("AGENT_SUMMARY_CHUNK_PAIRS", "10"))
AGENT_SUMMARY_MAX_TOKENS = int(os.getenv("AGENT_SUMMARY_MAX_TOKENS", "500"))

# Системный промпт для сворачивания истории агента в сводку — отдельный от основного
# SYSTEM_PROMPT (та роль — отвечать пользователю на инвестиционные вопросы, эта —
# техническая задача сжатия диалога без искажения фактов). Не инвестиционный совет,
# поэтому дисклеймеры из SYSTEM_PROMPT здесь не нужны.
AGENT_SUMMARY_SYSTEM_PROMPT = (
    "Ты сжимаешь историю диалога Telegram-бота с пользователем в краткую сводку для "
    "внутреннего использования другой моделью — сама сводка пользователю не показывается.\n\n"
    "Тебе дают текущую сводку более ранней части диалога (может быть пустой, если это "
    "первое сворачивание) и следующий по времени фрагмент диалога. Верни ОДНУ новую "
    "сводку, объединяющую и старую сводку, и новый фрагмент.\n\n"
    "Правила:\n"
    "1. Сохраняй все факты, значимые для продолжения диалога: о чём спрашивал "
    "пользователь, какие темы/активы/цели упоминались, какие ответы/рекомендации "
    "уже были даны.\n"
    "2. Не придумывай ничего, чего не было в диалоге.\n"
    "3. Пиши кратко и по-русски, без вступлений вида «вот сводка» — сразу текст сводки.\n"
    "4. Не включай в сводку номера счетов/карт, паспортные данные и другие "
    "чувствительные персональные данные, даже если пользователь их упоминал."
)

# Стратегии управления контекстом /agent (agents/agent.py) — переключаются командой
# /agent_context (agents/agent_command.py) и хранятся per-chat в JSON-файле истории,
# а не в .env: это выбор пользователя чата в рамках диалога с агентом, а не
# настройка оператора бота (в отличие от MAIN_CLIENT/MAIN_MODEL, переключение
# которых во время выполнения запрещено — см. «Ограничения безопасности» в
# CLAUDE.md; здесь речь только о том, как формируется контекст для уже выбранного
# провайдера/модели, а не о смене самого провайдера/модели/системного промпта).
# AGENT_CONTEXT_STRATEGY — стратегия по умолчанию для новых чатов и для миграции
# файлов истории в старом формате (см. Agent._load_state) — "summary", чтобы
# поведение уже существующих чатов не изменилось молча при обновлении бота.
_SUPPORTED_AGENT_STRATEGIES = ("summary", "sliding_window", "sticky_facts", "branching")
AGENT_CONTEXT_STRATEGY = os.getenv("AGENT_CONTEXT_STRATEGY", "summary").strip().lower()

# Человекочитаемые подписи стратегий для сообщений бота (кнопки /agent_context,
# приветствие /agent) — единственное место с этими подписями, чтобы не дублировать
# формулировки по разным модулям.
AGENT_STRATEGY_LABELS = {
    "summary": "Summary + хвост (сводка старой части + недавние сообщения)",
    "sliding_window": "Sliding Window (только последние сообщения)",
    "sticky_facts": "Sticky Facts (факты диалога + последние сообщения)",
    "branching": "Branching (чекпоинты и ветки диалога)",
}

# AGENT_ENABLED_STRATEGIES — какие из 4 стратегий вообще предлагаются пользователю
# кнопками /agent_context (agents/agent_command.py: _strategy_keyboard() показывает
# только их, agent_context_callback() отклоняет попытку переключиться на
# отключённую стратегию). Это решение оператора бота — например, временно скрыть
# ещё не обкатанную стратегию, а не то, что пользователь чата может менять сам.
# Не ограничивает поведение Agent: если у чата уже сохранена стратегия, отсутствующая
# в этом списке (например, список сузили уже после того, как чат её выбрал), Agent
# продолжает работать по ней как обычно — список влияет только на набор кнопок,
# предлагаемых для переключения, а не на то, какая стратегия допустима в принципе
# (это по-прежнему _SUPPORTED_AGENT_STRATEGIES/AGENT_STRATEGY_LABELS). По умолчанию
# — все 4, через запятую в AGENT_ENABLED_STRATEGIES; AGENT_CONTEXT_STRATEGY обязан
# входить в этот список (см. _validate_config), иначе новые чаты стартовали бы со
# стратегией, которую сами не могут ни увидеть, ни осознанно выбрать повторно.
AGENT_ENABLED_STRATEGIES = tuple(
    name.strip().lower()
    for name in os.getenv(
        "AGENT_ENABLED_STRATEGIES", ",".join(_SUPPORTED_AGENT_STRATEGIES)
    ).split(",")
    if name.strip()
)

# Sliding Window и Sticky Facts используют один и тот же размер "окна" последних пар
# вопрос-ответ — AGENT_CONTEXT_RECENT_PAIRS (см. выше), он же используется как размер
# несворачиваемого хвоста в стратегии summary, поэтому отдельной переменной для окна
# не заводим.

# Системный промпт для стратегии Sticky Facts / Key-Value Memory: вместо пересказа
# (как в AGENT_SUMMARY_SYSTEM_PROMPT) модель ведёт структурированный словарь
# ключ-значение с устойчивыми фактами диалога (цель, ограничения, предпочтения,
# решения, договорённости) — обновляется после каждого вопроса пользователя (см.
# Agent._update_facts), а не раз в чанк, как summary. Просим строго JSON-объект без
# обрамляющего текста, чтобы результат можно было напрямую разобрать как словарь.
# Правило 6 (лимит на число ключей) — защита от переполнения AGENT_FACTS_MAX_TOKENS:
# без явного потолка словарь фактов растёт неограниченно, и рано или поздно ответ
# модели обрезается посередине JSON (JSONDecodeError "Unterminated string") — см.
# Agent._update_facts() про то, как это дополнительно подстраховано на стороне кода
# (бэклог необработанных пар + один повтор с удвоенным max_tokens), но структурное
# ограничение размера всё равно нужно, иначе проблема со временем возвращается.
AGENT_FACTS_SYSTEM_PROMPT = (
    "Ты ведёшь структурированную память диалога Telegram-бота с пользователем — "
    "словарь ключ-значение с устойчивыми фактами, полезными для продолжения "
    "разговора. Эта память не показывается пользователю напрямую.\n\n"
    "Тебе дают текущий словарь фактов в формате JSON (может быть пустым {}, если "
    "это первое обновление) и новые пары вопрос-ответ, ещё не учтённые в словаре. "
    "Верни ОБНОВЛЁННЫЙ словарь фактов одним JSON-объектом (ключи и значения — "
    "строки на русском языке).\n\n"
    "Правила:\n"
    "1. Извлекай и сохраняй факты вида: цель пользователя, ограничения "
    "(например, срок, сумма, готовность к риску), предпочтения, уже принятые "
    "решения и договорённости.\n"
    "2. Если новая пара вопрос-ответ уточняет или заменяет уже известный факт — "
    "обнови значение существующего ключа, а не добавляй противоречивый дубликат.\n"
    "3. Если в новых парах нет новых устойчивых фактов — верни словарь без "
    "изменений.\n"
    "4. Не придумывай факты, которых не было в диалоге.\n"
    "5. Не включай номера счетов/карт, паспортные данные и другие чувствительные "
    "персональные данные, даже если пользователь их упоминал.\n"
    "6. Держи словарь компактным — не более примерно 20 ключей. Если фактов "
    "становится больше, объединяй близкие по смыслу в один ключ или заменяй менее "
    "важные более актуальными, а не накапливай бесконечно.\n"
    "7. Верни ТОЛЬКО JSON-объект, без пояснений и без markdown-разметки вокруг "
    "него."
)
# Поднят с прежних 500: словарь фактов может занимать заметную часть JSON-ответа, и
# при более-менее насыщенном диалоге 500 токенов не хватало на весь словарь целиком
# (ответ обрезался посередине строки, JSONDecodeError) — см. правило 6 выше и
# Agent._update_facts() про дополнительный повтор с удвоенным лимитом при сбое.
AGENT_FACTS_MAX_TOKENS = int(os.getenv("AGENT_FACTS_MAX_TOKENS", "1500"))

# Системный промпт для /agent_compare_report (agents/compare_command.py) — сравнивает
# последние ответы трёх фиксированных стратегий (sliding_window/sticky_facts/branching,
# summary сюда не входит) по тем же критериям, что обсуждались как метрики сравнения:
# качество ответа, устойчивость (не потеряны ли важные детали из более раннего
# контекста) и расход токенов. Не инвестиционный совет, поэтому дисклеймеры из
# SYSTEM_PROMPT здесь не нужны — как и AGENT_SUMMARY_SYSTEM_PROMPT/
# AGENT_FACTS_SYSTEM_PROMPT, это техническая задача для отдельного вызова, а не ответ
# пользователю на инвестиционный вопрос.
AGENT_COMPARE_REPORT_SYSTEM_PROMPT = (
    "Тебе дают три ответа одного и того же LLM-агента на один и тот же диалог с "
    "пользователем — каждый ответ получен со своей стратегией управления контекстом "
    "(Sliding Window, Sticky Facts, Branching). Сравни их и сформулируй краткий отчёт "
    "на русском языке по трём критериям:\n"
    "1. Качество ответа — насколько ответ по существу, точен и полезен пользователю.\n"
    "2. Устойчивость — не потерял ли ответ важные детали, которые упоминались раньше "
    "в диалоге (суммы, сроки, ограничения, предпочтения пользователя и т.п.).\n"
    "3. Расход токенов — если в сообщении с ответами указана статистика по токенам, "
    "сравни её; если не указана, пропусти этот пункт без домыслов.\n\n"
    "Не придумывай ничего, чего нет в переданных ответах. Пиши кратко, по существу, "
    "структурированно (например, по каждому критерию отдельным пунктом)."
)

# --- Память smart-агента (/smart_agent, agents/smart_agent.py) ---
# В отличие от Agent (/agent), где вся история активной ветки подаётся в LLM через
# одну из 4 переключаемых стратегий, SmartAgent хранит три явно разделённых слоя
# памяти — краткосрочную (диалог), рабочую (данные текущей задачи) и долговременную
# (факты, сохранённые пользователем явно) — и пользователь сам решает командами
# /smart_agent_*, что и куда сохраняется, а не одна эвристика решает за него (см.
# «Управление памятью smart-агента» в CLAUDE.md). Поверх этих трёх слоёв — именованные
# профили персонализации (/smart_agent_profile): каждый профиль хранит СВОИ
# независимые short_term/working/long_term плюс явно заданные предпочтения (стиль,
# формат и т.п., см. PROFILE_FIELDS в agents/smart_agent.py) — переключение профиля
# переключает всю память целиком, как переключение ветки Branching у Agent. Отдельный
# от AGENT_HISTORY_DIR каталог — файлы независимого агента не должны путаться с
# файлами истории /agent того же чата.
AGENT_MEMORY_DIR = os.getenv("AGENT_MEMORY_DIR", "data/smart_agent_memory")

# Сколько последних пар вопрос-ответ краткосрочного слоя уходит в контекст LLM (как
# AGENT_CONTEXT_RECENT_PAIRS для стратегии sliding_window у /agent) — сама
# краткосрочная память на диске при этом не обрезается, обрезается только то, что
# уходит в конкретный вызов (см. SmartAgent._recent_short_term).
AGENT_MEMORY_SHORT_TERM_PAIRS = int(os.getenv("AGENT_MEMORY_SHORT_TERM_PAIRS", "10"))

# Максимум фактов в долговременной памяти — простой лимит по количеству, а не по
# токенам (в отличие от AGENT_FACTS_MAX_TOKENS у /agent): факты добавляются
# дословно, без LLM-обработки, которую нужно было бы защищать от обрезки ответа.
# При превышении вытесняется самый старый факт, но сначала — автоматически
# извлечённые: факт, который пользователь сохранил руками (/smart_agent_remember),
# не должен вытесняться автоматикой (см. SmartAgent.remember).
AGENT_MEMORY_LONG_TERM_MAX_FACTS = int(os.getenv("AGENT_MEMORY_LONG_TERM_MAX_FACTS", "20"))

# --- Конечный автомат рабочей задачи smart-агента (agents/task_state.py) ---
# Рабочая задача /smart_agent — не просто «цель + словарь данных», а конечный
# автомат: этап (planning -> execution -> validation -> done, с откатом
# validation -> execution), текущий шаг и ожидаемое действие. Двигает его
# ОТДЕЛЬНЫЙ технический вызов LLM после каждого ответа агента — то самое
# осознанное исключение из принципа «в рабочую и долговременную память пишет
# только пользователь явно» (см. «Управление памятью smart-агента» в CLAUDE.md).
# Сам переход при этом разрешает не модель, а код: условие этапа — заполненность
# его обязательных ключей data (agents/task_state.py, missing_keys).
AGENT_TASK_STATE_SYSTEM_PROMPT = (
    "Ты ведёшь СЛУЖЕБНОЕ состояние рабочей задачи Telegram-бота по инвестициям. "
    "Твой ответ пользователю не показывается — его разбирает код.\n\n"
    "Тебе дают описание сценария и его этапов, текущее состояние задачи в JSON, "
    "список допустимых ключей данных и новые пары вопрос-ответ, ещё не учтённые в "
    "состоянии. Верни ОДИН JSON-объект с полями:\n"
    "- \"stage\" — оставляй ТЕКУЩИЙ этап. Вперёд задачу переводит система сама, как "
    "только все обязательные ключи этапа заполнены — просить об этом не нужно. "
    "Указывай здесь более РАННИЙ этап только тогда, когда пользователь хочет "
    "вернуться и переделать уже пройденное;\n"
    "- \"step\" — коротко (до 15 слов), что делается прямо сейчас;\n"
    "- \"expected_action\" — коротко, чего ждём дальше (например, ответа "
    "пользователя на конкретный вопрос);\n"
    "- \"data_updates\" — объект ТОЛЬКО из перечисленных допустимых ключей, "
    "значения — строки на русском;\n"
    "- \"new_facts\" — массив строк с устойчивыми фактами о пользователе, которые "
    "останутся полезными и после этой задачи (может быть пустым).\n\n"
    "Правила:\n"
    "1. Не придумывай данные, которых не было в диалоге. Если пользователь на "
    "вопрос не ответил — не заполняй соответствующий ключ.\n"
    "2. Продвижение задачи — это заполнение ключей, а не смена \"stage\": заполняй "
    "ключ, как только пользователь дал по нему ответ, и не заполняй, пока не дал. "
    "Если пользователь просит перейти дальше, а данных не хватает — просто не "
    "заполняй недостающие ключи.\n"
    "3. Ключ \"verdict\" — это и есть итог этапа проверки, заполняй его сразу, как "
    "только позиция пользователя ясна, строго одним из двух значений: "
    "\"accepted\" (согласен с результатом, задача завершается) или "
    "\"changes_requested\" (просит правки, задача вернётся к работе над "
    "результатом). Любое другое значение будет отброшено, и задача застрянет на "
    "проверке.\n"
    "4. Никогда не сохраняй номера счетов и карт, паспортные данные, ИНН и "
    "денежные суммы. Состав и структуру портфеля фиксируй только в долях или "
    "процентах.\n"
    "5. В \"new_facts\" включай только то, что переживёт эту задачу (устойчивые "
    "предпочтения и ограничения пользователя), а не промежуточные детали.\n"
    "6. Верни ТОЛЬКО JSON-объект, без пояснений и без markdown-разметки."
)
AGENT_TASK_STATE_MAX_TOKENS = int(os.getenv("AGENT_TASK_STATE_MAX_TOKENS", "800"))

# Отдельный, намеренно более дешёвый вызов для случая, когда активной задачи нет:
# нужно решить только, начинает ли пользователь задачу, и если да — какую. Иначе
# каждый разовый вопрос («что такое ETF») тянул бы за собой полноценный разбор
# состояния по промпту выше.
AGENT_TASK_START_SYSTEM_PROMPT = (
    "Ты определяешь, начинает ли пользователь Telegram-бота по инвестициям новую "
    "рабочую задачу из фиксированного списка сценариев. Твой ответ пользователю не "
    "показывается — его разбирает код.\n\n"
    "Тебе дают список доступных сценариев и последние пары вопрос-ответ. Верни "
    "ОДИН JSON-объект с полями:\n"
    "- \"task_type\" — ключ подходящего сценария или \"none\";\n"
    "- \"goal\" — одна короткая фраза с целью задачи на русском (пустая строка, "
    "если task_type = \"none\").\n\n"
    "Правила:\n"
    "1. \"none\" — значение по умолчанию. Начинай задачу, только если пользователь "
    "явно просит проделать работу из сценария (например: «хочу составить "
    "портфель», «разбери актив X», «посмотри мой текущий портфель»).\n"
    "2. Обычный вопрос за справкой («что такое ETF», «чем облигации отличаются от "
    "акций») — это НЕ задача.\n"
    "3. Не придумывай цель, которой нет в словах пользователя.\n"
    "4. Не включай в \"goal\" денежные суммы, номера счетов и карт и другие "
    "чувствительные данные.\n"
    "5. Верни ТОЛЬКО JSON-объект, без пояснений и без markdown-разметки."
)
AGENT_TASK_START_MAX_TOKENS = int(os.getenv("AGENT_TASK_START_MAX_TOKENS", "200"))

# --- Инварианты smart-агента (agents/invariants.py) ---
# Жёсткие ограничения пользователя, которые агент не имеет права нарушать («без
# криптовалют», «доля акций не выше 40%»). Отдельный слой памяти, а не факт и не поле
# профиля: он уходит в контекст первым, с приоритетом над профилем, а результат
# рабочей задачи дополнительно проверяется на него ОТДЕЛЬНЫМ вызовом-ревизором — и
# решение откатить задачу на доработку принимает код, а не модель в свободном тексте
# (см. «Инварианты smart-агента» в CLAUDE.md).
#
# Лимит — простой, по количеству, как AGENT_MEMORY_LONG_TERM_MAX_FACTS, но с важным
# отличием: при переполнении НИЧЕГО не вытесняется, а добавление отклоняется. Молча
# выбросить из памяти запрет, который пользователь задал явно, нельзя — он перестал бы
# действовать незаметно для него.
AGENT_MEMORY_MAX_INVARIANTS = int(os.getenv("AGENT_MEMORY_MAX_INVARIANTS", "10"))

# Потолок длины одной формулировки: инвариант должен быть проверяемым правилом в одну
# фразу, а не абзацем рассуждений — иначе его нечем проверить ни модели, ни ревизору.
AGENT_INVARIANT_MAX_CHARS = int(os.getenv("AGENT_INVARIANT_MAX_CHARS", "300"))

# Промпт вызова-ревизора: проверяет ГОТОВЫЙ результат рабочей задачи (структуру
# портфеля, разбор, предлагаемые изменения) на соответствие инвариантам. Не вынесен в
# .env по тому же принципу, что AGENT_TASK_STATE_SYSTEM_PROMPT/
# AGENT_TASK_START_SYSTEM_PROMPT. Правило 1 (сомнение — в пользу "ok") намеренно
# смещает ревизора в сторону ложных пропусков, а не ложных срабатываний: ложное
# нарушение отправляет задачу на доработку и выглядит для пользователя как
# необъяснимое упрямство бота, а пропущенное — ловится тем, что инварианты и так
# лежат в контексте основного ответа.
AGENT_INVARIANTS_SYSTEM_PROMPT = (
    "Ты — СЛУЖЕБНЫЙ ревизор результата рабочей задачи Telegram-бота по инвестициям. "
    "Твой ответ пользователю не показывается — его разбирает код.\n\n"
    "Тебе дают пронумерованный список инвариантов (жёстких ограничений пользователя) "
    "и результат, который агент предложил по задаче. Проверь результат на "
    "соответствие КАЖДОМУ инварианту и верни ОДИН JSON-объект с полями:\n"
    "- \"verdict\" — \"ok\", если результат не нарушает ни одного инварианта, или "
    "\"violated\", если нарушает хотя бы один;\n"
    "- \"violations\" — массив объектов {\"index\": номер инварианта из списка, "
    "\"why\": одна короткая фраза на русском о том, что именно нарушено}. Пустой "
    "массив, если нарушений нет.\n\n"
    "Правила:\n"
    "1. Проверяй только то, что реально написано в результате. Не домысливай "
    "нарушения, которых в тексте нет: сомневаешься — возвращай \"ok\".\n"
    "2. Нарушение — это прямое противоречие формулировке инварианта, а не "
    "стилистическое несовпадение и не то, что инвариант в результате просто не "
    "упомянут.\n"
    "3. В \"index\" указывай номер из присланного списка и не придумывай новых.\n"
    "4. Ничего не советуй и не переписывай результат — от тебя нужна только "
    "проверка.\n"
    "5. Не включай в ответ денежные суммы, номера счетов и карт, паспортные данные "
    "и ИНН.\n"
    "6. Верни ТОЛЬКО JSON-объект, без пояснений и без markdown-разметки."
)
AGENT_INVARIANTS_MAX_TOKENS = int(os.getenv("AGENT_INVARIANTS_MAX_TOKENS", "400"))

# Telegram режет сообщения по 4096 символов — оставляем запас.
TELEGRAM_MESSAGE_LIMIT = 4000

# Задаёт и доменную роль бота (инвестиционные вопросы), и обязательные ограничения
# (см. раздел "Правила предметной области" в CLAUDE.md) — при доработке текста
# проверяй, что дисклеймер и осторожные формулировки не потерялись.
SYSTEM_PROMPT = (
    "Ты — ассистент Telegram-бота, отвечающий на вопросы пользователей об инвестициях "
    "и личных финансах. Отвечай кратко, по существу и на русском языке.\n\n"
    "Обязательно соблюдай следующие правила:\n"
    "1. Ты не являешься лицензированным финансовым советником, а твои ответы не являются "
    "индивидуальной инвестиционной рекомендацией. Если вопрос предполагает конкретное "
    "решение (купить/продать/во что вложить), явно напоминай об этом и советуй "
    "проконсультироваться с лицензированным финансовым консультантом перед принятием решения.\n"
    "2. Никогда не гарантируй доходность, рост стоимости активов или отсутствие риска. "
    "Не используй формулировки вида «точно вырастет», «гарантированная доходность», "
    "«безрисковый вариант» — любые инвестиции сопряжены с риском, и об этом нужно говорить "
    "прямо.\n"
    "3. Не запрашивай у пользователя чувствительные персональные данные: номера счетов и "
    "карт, паспортные данные, ИНН, точные суммы на счетах. Если пользователь сам их "
    "присылает, не проси их подтвердить или уточнить, и не включай в свой ответ.\n"
    "4. Если вопрос выходит за рамки инвестиций и личных финансов, вежливо сообщи об этом "
    "и предложи переформулировать вопрос в рамках этой темы."
)

logging.basicConfig(
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    level=logging.INFO,
)
# Библиотека httpx (используется и telegram, и openai) логирует каждый запрос —
# приглушаем, чтобы не засорять логи.
logging.getLogger("httpx").setLevel(logging.WARNING)

# python-telegram-bot предупреждает про каждый ConversationHandler, где в состояниях
# есть CallbackQueryHandler, а per_message=False (по умолчанию) — это ожидаемо и
# безопасно для наших research-режимов (research/constraints.py, reasoning.py,
# temperature.py, models.py): их состояния вперемешку принимают и текст (вопрос,
# max_tokens, стоп-слова), и нажатия inline-кнопок, а per_message=True требует, чтобы
# *все* обработчики (entry_points/states/fallbacks) были CallbackQueryHandler — что
# ломает ввод текста в этих режимах. Каждый чат ведёт только один активный диалог, так
# что отслеживание разговора по конкретному сообщению с кнопками (то, что даёт
# per_message=True) здесь не нужно — предупреждение только шумит в логах при старте.
warnings.filterwarnings(
    "ignore",
    message=r"If 'per_message=False', 'CallbackQueryHandler' will not be tracked for every message\.",
    category=PTBUserWarning,
)

logger = logging.getLogger(__name__)


def _validate_config() -> None:
    """Проверяет наличие обязательных переменных окружения перед стартом.

    Обязательность DEEPSEEK_API_KEY/KIMI_API_KEY зависит от MAIN_CLIENT и проверяется
    отдельно в providers/deepseek_client.py/providers/kimi_client.py (модуль выбранного провайдера сам
    завершает процесс, если его ключа нет) — здесь только TELEGRAM_BOT_TOKEN и сам
    MAIN_CLIENT, т.к. это переменные, за которые отвечает именно этот модуль.
    """
    if not TELEGRAM_BOT_TOKEN:
        logger.error(
            "Отсутствует обязательная переменная окружения TELEGRAM_BOT_TOKEN. "
            "Скопируйте .env.example в .env и заполните значение."
        )
        sys.exit(1)

    if _RESEARCH_RAW not in _TRUE_VALUES | _FALSE_VALUES:
        logger.error(
            "Недопустимое значение RESEARCH=%r. Допустимые значения: true/false "
            "(также принимаются 1/0, yes/no, on/off).",
            _RESEARCH_RAW,
        )
        sys.exit(1)

    if MAIN_CLIENT not in _SUPPORTED_MAIN_CLIENTS:
        logger.error(
            "Недопустимое значение MAIN_CLIENT=%r. Допустимые значения: %s.",
            MAIN_CLIENT,
            ", ".join(_SUPPORTED_MAIN_CLIENTS),
        )
        sys.exit(1)

    if AGENT_CONTEXT_STRATEGY not in _SUPPORTED_AGENT_STRATEGIES:
        logger.error(
            "Недопустимое значение AGENT_CONTEXT_STRATEGY=%r. Допустимые значения: %s.",
            AGENT_CONTEXT_STRATEGY,
            ", ".join(_SUPPORTED_AGENT_STRATEGIES),
        )
        sys.exit(1)

    invalid_enabled_strategies = [
        name for name in AGENT_ENABLED_STRATEGIES if name not in _SUPPORTED_AGENT_STRATEGIES
    ]
    if not AGENT_ENABLED_STRATEGIES or invalid_enabled_strategies:
        logger.error(
            "Недопустимое значение AGENT_ENABLED_STRATEGIES=%r. Список не может быть "
            "пустым, каждое значение — одно из: %s.",
            ", ".join(AGENT_ENABLED_STRATEGIES),
            ", ".join(_SUPPORTED_AGENT_STRATEGIES),
        )
        sys.exit(1)

    if AGENT_CONTEXT_STRATEGY not in AGENT_ENABLED_STRATEGIES:
        logger.error(
            "AGENT_CONTEXT_STRATEGY=%r должна входить в AGENT_ENABLED_STRATEGIES=%r — "
            "иначе новые чаты стартуют со стратегией, которую сами не смогут выбрать "
            "через /agent_context.",
            AGENT_CONTEXT_STRATEGY,
            ", ".join(AGENT_ENABLED_STRATEGIES),
        )
        sys.exit(1)


_validate_config()
