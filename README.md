# Investment Telegram Bot (DeepSeek / Kimi)

Telegram-бот-ассистент по инвестициям и личным финансам поверх
[DeepSeek API](https://platform.deepseek.com/) или [Kimi (Moonshot AI) API](https://platform.moonshot.ai/) —
провайдер и модель основного потока выбираются переменными окружения `MAIN_CLIENT`/
`MAIN_MODEL` (по умолчанию DeepSeek). Пользователь пишет вопрос в личном чате — бот
пересылает его выбранному провайдеру и возвращает ответ как есть.

Бот **не хранит историю обычных сообщений**: каждое из них обрабатывается как
независимый запрос. Это упрощает архитектуру, снижает расход токенов,
исключает утечку контекста между разными пользователями и не даёт финансовым
вопросам пользователя накапливаться без явной необходимости.

Единственное исключение — режим `/agent`: по явному запросу он запоминает
историю диалога конкретного чата в JSON-файле на диске и продолжает её даже
после перезапуска бота. Историю можно посмотреть (`/agent_history`) и в любой
момент полностью очистить (`/agent_reset`).

⚠️ Бот не является лицензированным финансовым советником, а его ответы не
являются индивидуальной инвестиционной рекомендацией и не гарантируют
доходность — это явно проговаривается пользователю в `/start` и `/help`.

## Возможности

- Приём текстовых сообщений в личных чатах Telegram (групповые чаты и
  не-текстовый контент игнорируются).
- Пересылка запроса выбранному провайдеру (DeepSeek или Kimi, OpenAI-совместимый
  интерфейс) с системным промптом, задающим доменную роль и обязательные
  дисклеймеры.
- Индикация статуса «печатает…» на время ожидания ответа.
- Команды `/start` и `/help`.
- `/agent` — LLM-агент с памятью диалога: в отличие от обычных сообщений,
  вопросы в этом режиме идут один за другим с учётом истории переписки
  конкретного чата. История хранится в JSON на диске (по одному файлу на
  chat_id, каталог `AGENT_HISTORY_DIR`) и переживает и повторный вход в
  `/agent`, и перезапуск бота — это единственное исключение из
  «бот не хранит историю» в проекте, включённое по явному запросу.
  Дополнительные команды: `/agent_history` печатает сохранённую историю,
  `/agent_reset` полностью её очищает. Выйти из диалогового режима (без
  удаления истории) можно кнопкой на клавиатуре или командой `/cancel`.
- Четыре технических research-режима — не для обычных вопросов, а для
  исследования поведения самого API; каждый доступен только в личном чате и
  завершается командой `/cancel`:
  - `/research_constraints` — влияние ограничений API на ответ: без ограничений
    (контроль), формат ответа (JSON-режим `response_format`), `max_tokens`,
    стоп-слова (`stop`) — для двух последних значение вводит сам пользователь.
  - `/research_reasoning` — сравнение способов рассуждения: прямой ответ,
    пошаговая инструкция, промпт, сгенерированный самой моделью, решение от
    лица одного из трёх экспертов (аналитик/инженер/критик), и сравнение всех
    способов сразу.
  - `/research_temperature` — сравнение фиксированных значений `temperature`
    (0 / 0.7 / 1.2 / 2 — весь допустимый диапазон API) на одной и той же
    задаче, и сравнение всех значений сразу по точности, креативности и
    разнообразию.
  - `/research_models` — сравнение четырёх моделей DeepSeek и Kimi на одной и
    той же задаче по качеству, скорости ответа и стоимости (иллюстративной, по
    цене за токены), и сравнение всех моделей сразу.

  После каждого сценария бот показывает минимальную статистику ответа
  (`finish_reason`, использованные токены; `/research_models` — ещё и время
  ответа и стоимость). Сообщения, ожидающие выбора или ввода от пользователя,
  помечены значком 👉 — в отличие от результатов сценария (значок 📊) и
  статистики (значок 📈).
- Обработка ошибок сети, аутентификации и превышения лимитов API с понятными
  сообщениями пользователю на русском языке.
- Никаких админ-команд и возможности сменить провайдера, модель или системный
  промпт из чата — это осознанное ограничение поверхности атаки; смена
  провайдера/модели — только через `.env` и перезапуск бота.
- Бот не запрашивает и не хранит чувствительные персональные/финансовые
  данные (номера счетов и карт, паспортные данные, ИНН и т.п.).

## Технологии

- Python 3.10+
- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) v20+
- [openai](https://github.com/openai/openai-python) SDK — используется как клиент для обоих провайдеров
  (DeepSeek и Kimi предоставляют OpenAI-совместимый интерфейс)
- [python-dotenv](https://github.com/theskumar/python-dotenv) для загрузки конфигурации

## Структура проекта

```
├── src/
│   ├── config.py                        # Переменные окружения, константы, SYSTEM_PROMPT, логирование
│   ├── providers/                       # Подключения к LLM-провайдерам
│   │   ├── deepseek_client.py           # Клиент DeepSeek
│   │   ├── kimi_client.py               # Клиент Kimi (Moonshot AI)
│   │   └── main_client.py               # Выбор основного клиента по MAIN_CLIENT
│   ├── research/                        # Технические режимы исследования API (/research_*)
│   │   ├── _shared.py                   # Общий каркас: ошибки API, статистика, /cancel
│   │   ├── constraints.py               # Режим /research_constraints
│   │   ├── reasoning.py                 # Режим /research_reasoning
│   │   ├── temperature.py               # Режим /research_temperature
│   │   └── models.py                    # Режим /research_models
│   ├── agents/                          # Агенты — LLM-цикл отдельно от Telegram-обработчиков
│   │   ├── agent.py                     # Класс Agent (память диалога в JSON на диске)
│   │   └── agent_command.py             # Команды /agent, /agent_history, /agent_reset
│   └── main.py                          # Обычный поток бота (/start, /help, ответы) и точка входа
├── tests/                   # Заглушка для будущих тестов
│   └── .gitkeep
├── data/agent_history/       # История диалогов /agent (JSON на chat_id) — создаётся при первом
│                              # использовании /agent, не коммитится (см. .gitignore)
├── .env.example              # Пример переменных окружения
├── .gitignore
├── .gitattributes
├── .pre-commit-config.yaml
├── requirements.txt
├── pyproject.toml
├── Makefile
├── LICENSE
└── README.md
```

## Установка и настройка

### 1. Клонируйте репозиторий и создайте виртуальное окружение

```bash
git clone <URL-этого-репозитория>
cd investment-telegram-bot
python3 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
```

### 2. Установите зависимости

```bash
make install
# или напрямую:
pip install -r requirements.txt
```

### 3. Получите токен Telegram-бота у BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather).
2. Отправьте команду `/newbot` и следуйте инструкциям (укажите имя и username бота).
3. BotFather пришлёт токен вида `123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`.
4. Сохраните этот токен — он понадобится на следующем шаге.

### 4. Получите API-ключ провайдера (DeepSeek и/или Kimi)

Основной поток бота работает через одного провайдера — того, что указан в
`MAIN_CLIENT` (по умолчанию `deepseek`). Ключ этого провайдера обязателен, ключ
второго — опционален и нужен только для сценариев `/research_models`, которые
всегда сравнивают модели обоих провайдеров.

**DeepSeek:**
1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com/).
2. Пополните баланс (при необходимости) в разделе биллинга.
3. Перейдите в раздел [API Keys](https://platform.deepseek.com/api_keys) и создайте новый ключ.

**Kimi (Moonshot AI):**
1. Зарегистрируйтесь на [platform.moonshot.ai](https://platform.moonshot.ai/).
2. Создайте API-ключ в личном кабинете.

Ключ показывается только один раз — сразу скопируйте его.

### 5. Настройте переменные окружения

```bash
cp .env.example .env
```

Откройте `.env` и заполните значения — как минимум токен бота и ключ выбранного
`MAIN_CLIENT`:

```dotenv
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather

MAIN_CLIENT=deepseek          # или "kimi"
MAIN_MODEL=deepseek-v4-flash  # модель основного потока для выбранного MAIN_CLIENT

DEEPSEEK_API_KEY=ваш_ключ_deepseek
KIMI_API_KEY=ваш_ключ_kimi    # опционален, если MAIN_CLIENT=deepseek
```

Полный список переменных (базовые URL, идентификаторы моделей для
`/research_models`, лимиты) — см. `.env.example`, он же источник истины по
значениям по умолчанию.

**Важно:** файл `.env` уже добавлен в `.gitignore` — никогда не коммитьте его
и не публикуйте содержащиеся в нём ключи.

Параметры технических research-режимов (структура JSON-ответа и лимит
стоп-слов в `/research_constraints`, значения `temperature` в
`/research_temperature`) в `.env` не выносятся — они заданы константами в
соответствующих модулях `src/research/`, так как являются предметом самого
исследования, а не настройкой поведения бота. Исключение — идентификаторы
моделей для `/research_models` (`DEEPSEEK_MODEL_PRO`, `DEEPSEEK_MODEL_FLASH`,
`KIMI_MODEL_K3`, `KIMI_MODEL_K2_6`): они вынесены в `.env`, потому что могут
меняться быстрее кода бота.

### 6. (Опционально) Настройте pre-commit хуки

Хуки проверяют код перед коммитом, включая поиск случайно закоммиченных секретов:

```bash
pip install pre-commit detect-secrets
detect-secrets scan > .secrets.baseline
pre-commit install
```

## Запуск

```bash
make run
# или напрямую:
python src/main.py
```

При успешном запуске в логах появится сообщение:

```
INFO - __main__ - Бот запущен. Провайдер: DeepSeek, модель: deepseek-v4-flash
```

Откройте бота в Telegram и отправьте `/start`.

## Пример работы

```
Пользователь: /start
Бот: 👋 Привет! Я бот-ассистент по инвестициям и личным финансам на базе
     DeepSeek AI (модель deepseek-v4-flash).

     Просто напиши свой вопрос об инвестициях, накоплениях или личных
     финансах — я перешлю его в DeepSeek и пришлю тебе ответ.

     ⚠️ Важно: я не являюсь лицензированным финансовым советником, а мои
     ответы — не индивидуальная инвестиционная рекомендация. Перед принятием
     решений проконсультируйся с лицензированным финансовым консультантом.

     🔒 Обычные сообщения я не запоминаю: каждое обрабатывается независимо от
     предыдущих. Исключение — режим /agent: там я сохраняю историю диалога на
     диске, чтобы отвечать с учётом предыдущих вопросов, даже после моего
     перезапуска; очистить её можно командой /agent_reset. В любом режиме не
     присылай, пожалуйста, номера счетов, карт и другие чувствительные данные.

     Используй /help, чтобы посмотреть список команд.

Пользователь: Стоит ли сейчас вкладываться в облигации или в акции?
Бот: (печатает...)
Бот: Выбор между облигациями и акциями зависит от твоего горизонта
     инвестирования и готовности к риску. Облигации обычно менее волатильны,
     но их доходность ниже; акции потенциально доходнее, но и рискованнее.
     Диверсификация между классами активов часто снижает общий риск портфеля.
     Это не индивидуальная инвестиционная рекомендация — перед решением
     проконсультируйся с лицензированным финансовым консультантом.
```

### Режим `/agent`

В отличие от обычных сообщений выше, `/agent` запоминает историю этого диалога
и учитывает её в каждом следующем ответе — в том числе после перезапуска бота:

```
Пользователь: /agent
Бот: 🤖 Режим агента.

     Задавай вопросы — агент помнит историю этого диалога и передаёт её в
     DeepSeek при каждом следующем вопросе, даже после перезапуска бота.

     👉 Введи вопрос. Чтобы выйти, нажми кнопку внизу (или отправь /cancel).
     Команда /agent_reset в любой момент очищает историю.
     [🚪 Выйти из режима агента]

Пользователь: Хочу собрать портфель из акций и облигаций. С чего начать?
Бот: (печатает...)
Бот: Начни с определения горизонта инвестирования и приемлемого уровня риска
     — от них будет зависеть соотношение акций и облигаций в портфеле. Это не
     индивидуальная инвестиционная рекомендация.

Пользователь: А если горизонт — 10 лет?
Бот: (печатает...)
Бот: При горизонте в 10 лет обычно допустима большая доля акций, так как
     есть время переждать просадки рынка — но точное соотношение зависит от
     твоей готовности к риску. Проконсультируйся с лицензированным
     финансовым консультантом перед принятием решения.

Пользователь: /agent_history
Бот: 📜 История диалога с агентом (2 вопрос(ов)):

     1. 🙋 Хочу собрать портфель из акций и облигаций. С чего начать?
     🤖 Начни с определения горизонта инвестирования и приемлемого уровня риска...

     2. 🙋 А если горизонт — 10 лет?
     🤖 При горизонте в 10 лет обычно допустима большая доля акций...

Пользователь: /agent_reset
Бот: 🗑 История диалога с агентом очищена. Следующий вопрос агент увидит как первый.
```

### Режим `/research_constraints`

```
Пользователь: /research_constraints
Бот: 🔬 Режим исследования влияния ограничений DeepSeek API (включая формат ответа) на ответ.
     👉 Введите вопрос для исследования влияния ограничений на ответ.

Пользователь: Дай рекомендацию по акциям Apple
Бот: Вопрос сохранён.

     👉 Выберите сценарий запроса к DeepSeek:
     [1️⃣ Без ограничений]  [2️⃣ Формат ответа (JSON)]
     [3️⃣ Max tokens]       [4️⃣ Свои стоп-слова]
     [❓ Новый вопрос]
     [🚪 Выйти из исследования]

Пользователь: (нажимает "2️⃣ Формат ответа (JSON)")
Бот: 📊 Сценарий: Формат ответа: JSON-режим (структура задана в промпте)
Бот: ```json
     {
       "companies": [
         {
           "ticker": "SBER",
           "company_name": "ПАО Сбербанк",
           "sector": "Финансовый сектор",
           "summary": "..."
         }
       ]
     }
     ```
Бот: 📈 finish_reason=stop, prompt_tokens=142, completion_tokens=38, total_tokens=180
Бот: 👉 Выберите следующий сценарий или завершите исследование:
     [1️⃣ Без ограничений]  [2️⃣ Формат ответа (JSON)] ...

Пользователь: (нажимает "3️⃣ Max tokens")
Бот: 👉 Введите максимальное количество токенов ответа (max_tokens, целое число):
Пользователь: 20
Бот: 📊 Сценарий: Ограничение max_tokens (max_tokens=20)
Бот: Apple — технологическая компания с сильной экосистемой продуктов и

     ⚠️ ВНИМАНИЕ: Ответ был ОБРЕЗАН из-за ограничения max_tokens!
Бот: 📈 finish_reason=length, prompt_tokens=138, completion_tokens=20, total_tokens=158
Бот: 👉 Выберите следующий сценарий или завершите исследование: ...

Пользователь: (нажимает "4️⃣ Свои стоп-слова")
Бот: 👉 Введите стоп-слова через запятую (не более 4), например: точка, конец
Пользователь: риск
Бот: 📊 Сценарий: Свои стоп-слова (риск)
Бот: Apple остаётся одним из лидеров технологического сектора, однако стоит учитывать
Бот: 📈 finish_reason=stop, prompt_tokens=140, completion_tokens=12, total_tokens=152
Бот: 👉 Выберите следующий сценарий или завершите исследование: ...
```

### Остальные research-режимы (кратко)

`/research_reasoning`, `/research_temperature` и `/research_models` устроены
по тому же принципу (вопрос/задача → выбор сценария кнопками → результат +
статистика → следующий сценарий или выход), но с нейтральным системным
промптом — они исследуют поведение API на произвольных задачах, а не отвечают
на инвестиционные вопросы:

- **`/research_reasoning`**: 4 фиксированных способа рассуждения плюс сценарий
  5, который параллельно запускает все четыре (включая всех трёх экспертов по
  отдельности) и одним отдельным запросом просит модель сравнить ответы между
  собой.
- **`/research_temperature`**: 4 фиксированных значения `temperature` плюс
  сценарий 5, который сравнивает их по точности, креативности и разнообразию
  и даёт рекомендации, для каких задач подходит каждое значение.
- **`/research_models`**: 4 фиксированные модели (Kimi K3, DeepSeek V4 Pro,
  Kimi K2.6, DeepSeek V4 Flash) плюс сценарий 5, который сравнивает их по
  качеству, скорости и стоимости ответа.

## Команды бота

| Команда                  | Описание                                                                      |
|--------------------------|--------------------------------------------------------------------------------|
| `/start`                 | Приветственное сообщение и краткая инструкция                                  |
| `/help`                  | Справка по использованию и ограничениям бота                                   |
| `/agent`                 | LLM-агент с памятью диалога: вопросы один за другим, контекст сохраняется даже после перезапуска бота |
| `/agent_history`         | Показать сохранённую историю диалога с агентом                                 |
| `/agent_reset`           | Очистить историю диалога с агентом                                             |
| `/research_constraints`  | Технический режим сравнения ограничений API (включая формат ответа)            |
| `/research_reasoning`    | Технический режим сравнения способов рассуждения                              |
| `/research_temperature`  | Технический режим сравнения значений `temperature`                            |
| `/research_models`       | Технический режим сравнения моделей DeepSeek и Kimi                           |
| `/cancel`                | Досрочный выход из режима `/agent` или любого активного research-режима        |

## Ограничения и особенности

- Бот отвечает только на текстовые сообщения в **личных чатах** (групповые чаты игнорируются).
- Максимальная длина запроса пользователя ограничена переменной `MAX_INPUT_CHARS` (по умолчанию 4000 символов).
- История обычных сообщений не сохраняется — контекст предыдущих вопросов не
  учитывается (в каждом research-режиме вопрос/задача временно хранится только
  на время самого диалога и не переживает выход из режима). Исключение —
  `/agent`: его история сохраняется в JSON на диске без ограничения длины и
  переживает перезапуск бота, пока пользователь не очистит её `/agent_reset`.
- Пользователь не может сменить провайдера, модель, системный промпт или иные
  параметры генерации из чата — смена `MAIN_CLIENT`/`MAIN_MODEL` требует
  правки `.env` и перезапуска бота.
- Бот не запрашивает и не должен получать чувствительные персональные/финансовые данные
  (номера счетов и карт, паспортные данные, ИНН, точные суммы на счетах).

## Безопасность

- Все секреты (токен бота, API-ключи провайдеров) загружаются только из переменных окружения / `.env`.
- `.env` находится в `.gitignore` и не должен попадать в git.
- В коде нет захардкоженных ключей или админ-бэкдоров.
- Рекомендуется использовать `pre-commit` с `detect-secrets` для дополнительной защиты от случайных утечек.
- История диалогов `/agent` (переписка пользователей, потенциально на финансовые
  темы) хранится в `data/agent_history/` — этот каталог тоже в `.gitignore` и не
  должен попадать в git или на публичный хостинг; путь настраивается через
  `AGENT_HISTORY_DIR`. Пользователь может удалить свою историю сам (`/agent_reset`).

## Лицензия

Проект распространяется под лицензией [MIT](LICENSE).
